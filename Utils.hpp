#ifndef UTILS_HPP
#define UTILS_HPP

#include <string>
#include <vector>

std::string to_string(size_t number);
std::vector<std::string> ft_split(std::string string, std::string const & delimiter);

#endif 